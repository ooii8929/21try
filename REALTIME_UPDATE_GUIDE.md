# ⚡ 即時更新功能說明

## 新功能：即時距離更新

現在 Bear Distance 會**即時更新**，不需要離開再回來！

## 工作原理

### 自動更新機制
- 每 **1 秒** 檢查一次距離
- 如果距離改變，自動更新 UI
- 不需要手動刷新頁面

### 測試模式下的行為

```
時間軸（停留在頁面上）：

0s:  距離 5/5 (綠色)
10s: 距離自動變成 4/5 (橘色) ⚡ 即時更新！
20s: 距離自動變成 3/5 (橘色) ⚡
30s: 距離自動變成 2/5 (橘色) ⚡
40s: 距離自動變成 1/5 (紅色) ⚡
50s: 距離自動變成 0/5 (紅色) ⚡ + 顯示警告通知
```

## 使用情境

### 情境 1: 停留在頁面觀察 👀

```
1. 打卡 → 距離 5/5
2. 停留在頁面上
3. 10 秒後 → 距離自動變成 4/5（不需要刷新）
4. 20 秒後 → 距離自動變成 3/5
5. ...持續更新
```

### 情境 2: 時間剛好到了要打卡 ⏰

```
場景：上次打卡是 9 秒前

1. 你在頁面上，距離顯示 5/5
2. 1 秒後（總共 10 秒）
3. 距離自動變成 4/5 ⚡
4. 你看到距離變了，立刻點擊打卡
5. 系統重置距離為 5/5
6. 顯示 "Great Job!" modal
```

**不會卡住！** 打卡時會重新計算並重置為 5。

### 情境 3: 距離降到 0 時 🐻

```
1. 距離是 1/5
2. 10 秒後自動變成 0/5
3. 同時顯示紅色通知：
   "⚠️ Bear caught you! Distance dropped to 0!"
4. 你可以立刻打卡重置
```

## 視覺效果

### 即時變化
- 距離數字即時更新（5/5 → 4/5 → 3/5...）
- 進度條即時變化
- 顏色即時改變（綠 → 橘 → 紅）
- 狀態文字即時更新

### 警告通知
當距離降到 0 時，會顯示：
```
┌─────────────────────────────────────┐
│ ⚠️ Bear caught you!                 │
│ Distance dropped to 0!              │
└─────────────────────────────────────┘
```

## 性能考量

### 更新頻率
- **檢查間隔**: 每 1 秒
- **實際更新**: 只在距離改變時更新 UI
- **資源消耗**: 極低（只是讀取 SharedPreferences）

### 自動清理
- 離開頁面時自動停止計時器
- 不會造成記憶體洩漏
- 不會影響其他頁面

## 測試方式

### 快速測試（10 秒模式）

1. **重置狀態**
   - 點擊 Bear Distance Card 上的 🔄 按鈕

2. **打卡**
   - 點擊 ✓ 快速打卡
   - 距離顯示 5/5

3. **觀察即時更新**
   - 停留在頁面上
   - 10 秒後看到距離變成 4/5
   - 20 秒後變成 3/5
   - 30 秒後變成 2/5（顏色變橘色）
   - 40 秒後變成 1/5（顏色變紅色）
   - 50 秒後變成 0/5（顯示警告通知）

4. **測試打卡重置**
   - 在任何時候點擊打卡
   - 距離立刻重置為 5/5

### 測試邊界情況

**測試 1: 在距離變化瞬間打卡**
```
1. 距離 5/5，已經過了 9 秒
2. 等待 1 秒（距離即將變成 4/5）
3. 在距離變成 4/5 的瞬間點擊打卡
4. 結果：距離重置為 5/5（打卡優先）
```

**測試 2: 距離降到 0 後打卡**
```
1. 等待 50 秒，距離變成 0/5
2. 看到警告通知
3. 點擊打卡
4. 結果：距離重置為 5/5，顯示 "Great Job!"
```

**測試 3: 快速切換頁面**
```
1. 打卡後立刻離開頁面
2. 等待 10 秒
3. 回到頁面
4. 結果：距離顯示 4/5（正確計算）
```

## 與之前的差異

### 之前（需要刷新）
```
打卡 → 等 10 秒 → 距離仍是 5/5
     → 離開頁面 → 回來 → 距離變成 4/5
```

### 現在（即時更新）
```
打卡 → 等 10 秒 → 距離自動變成 4/5 ⚡
```

## 技術細節

### 計時器實作
```dart
Timer.periodic(Duration(seconds: 1), (timer) async {
  final newDistance = await BearService.getCurrentDistance();
  if (newDistance != _currentDistance) {
    setState(() {
      _currentDistance = newDistance;
    });
  }
});
```

### 生命週期管理
- `initState()`: 啟動計時器
- `dispose()`: 停止計時器
- `mounted` 檢查: 確保安全更新

### 效能優化
- 只在距離改變時才更新 UI
- 使用 `mounted` 檢查避免錯誤
- 自動清理資源

## 常見問題

### Q: 會不會很耗電？
A: 不會。每秒只是讀取一個數值，消耗極低。

### Q: 如果我切換到其他 app？
A: 計時器會繼續運行，但當你回來時會正確顯示距離。

### Q: 可以關閉即時更新嗎？
A: 可以。註解掉 `_startDistanceUpdateTimer()` 即可。

### Q: 正式模式下也會即時更新嗎？
A: 會，但因為是以「天」為單位，所以不太會看到即時變化。主要是測試模式下比較明顯。

## 調整更新頻率

如果想改變檢查頻率，編輯 `goal_detail_screen.dart`:

```dart
// 每 0.5 秒檢查一次（更即時）
Timer.periodic(const Duration(milliseconds: 500), ...)

// 每 2 秒檢查一次（省電）
Timer.periodic(const Duration(seconds: 2), ...)

// 每 5 秒檢查一次（更省電）
Timer.periodic(const Duration(seconds: 5), ...)
```

## 建議

### 測試模式
- 保持 1 秒更新頻率（可以清楚看到變化）

### 正式模式
- 可以改成 5-10 秒更新頻率（因為是以天為單位）
- 或者完全關閉即時更新，只在進入頁面時檢查

## 總結

✅ **即時更新**: 不需要刷新頁面
✅ **視覺回饋**: 距離、顏色、文字即時變化
✅ **警告通知**: 距離降到 0 時自動提醒
✅ **效能優化**: 只在需要時更新 UI
✅ **自動清理**: 離開頁面時停止計時器

現在你可以停留在頁面上，看著距離即時變化了！🎉
