name: twenty_one_try
description: A goal tracking app based on the 21 try method.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  camera: ^0.11.3
  video_player: ^2.7.0
  chewie: ^1.8.1
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0     # 適用 Flutter 專案
  path_provider: ^2.1.3
  uuid: ^4.5.1
  photo_manager: ^3.0.0   # 若要用 assetId 還原影片

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.9

flutter:
  uses-material-design: true

  fonts:
    - family: Manrope
      fonts:
        - asset: fonts/Manrope-Regular.ttf
          weight: 400
        - asset: fonts/Manrope-Medium.ttf
          weight: 500
        - asset: fonts/Manrope-Bold.ttf
          weight: 700
